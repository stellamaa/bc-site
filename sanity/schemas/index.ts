import about from "./about-schema";
import category from "./category-schema";
import landingPage from "./landingPage-schema";
import logo from "./logo-schema";
import talent from "./talent-schema";
import work from "./work-schema";
import workPage from "./workPage-schema";

const schemas = [landingPage, workPage, about, category, work, talent, logo];

export default schemas;
