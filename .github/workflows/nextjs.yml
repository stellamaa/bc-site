# Retired — the live site deploys on Netlify (https://blank-co.uk).
# Kept as a manual-only stub so old Pages deploys don't fail on every push.
name: Deploy Next.js site to Pages (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Netlify only
        run: |
          echo "GitHub Pages deploy is disabled."
          echo "Use Netlify for builds and Sanity rebuild webhooks."
